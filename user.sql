use hello;
-- create table user(
-- staff_id int,
-- room_id int,
-- event_name varchar(255),
-- target_audi varchar(255),
-- num_attend int,
-- constraint attend_const check(num_attend <= 500));


-- alter table user add constraint user_staff_fk foreign key(staff_id) references staff(staff_id);
-- alter table user add constraint user_room_fk foreign key(room_id) references room(room_id);

